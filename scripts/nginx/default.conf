# server {
#     listen 80;
#     server_name testalb.erp360.strativ.se;

#     location / {
#         proxy_pass http://localhost:8001;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#     }
# }
# 1. Existing domain
# Default / fallback server
# server {
#     listen 80 default_server;
#     server_name _;  # matches any hostname

#     location / {
#         # You can either:
#         # 1. Redirect to a main domain
#         return 302 http://testalb.erp360.strativ.se;

#         # Or 2. Serve a static fallback page
#         # root /var/www/html;
#         # index index.html;
#     }
# }

# server {
#     listen 80;
#     server_name testalb.erp360.strativ.se;

#     location / {
#         proxy_pass http://localhost:8001;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#     }
# }

# # 2. abc.com → port 8002
# server {
#     listen 80;
#     server_name testalbx.erp360.strativ.se;

#     location / {
#         proxy_pass http://localhost:8002;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#     }
# }

# # 3. xmy.com → port 8003
# server {
#     listen 80;
#     server_name testalby.erp360.strativ.se;

#     location / {
#         proxy_pass http://localhost:8003;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#     }
# }

# # 4. mxt.com → port 8004
# server {
#     listen 80;
#     server_name testalbz.erp360.strativ.se;

#     location / {
#         proxy_pass http://localhost:8004;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#     }
# }

user  root;
# user nginx;

worker_processes  auto;

events {
    worker_connections 1024;
}

http {
    include       /etc/nginx/mime.types;
    default_type  application/octet-stream;

    # Logging
    access_log  /var/log/nginx/access.log;
    error_log   /var/log/nginx/error.log;

    sendfile        on;
    keepalive_timeout  65;

    # Your domains
    server {
        listen 80;
        server_name testalb.erp360.strativ.se;
        location / {
            proxy_pass http://localhost:8001;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;   # <-- Add this line

        }
    }

    server {
        listen 80;
        server_name testv2.erp360.strativ.se;
        location / {
            proxy_pass http://localhost:8005;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        }
    }
    # Default fallback
    server {
        listen 80 default_server;
        server_name _;
        return 302 http://testalbz.erp360.strativ.se;
    }
}
